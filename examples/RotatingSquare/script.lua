function RotateBoxes(entity, alpha, beta, gama)
    print()
    print(1 + 2 + 3 + gama.value)
    lambda = "Hello, World!"

    if cucumber then
        doSomething()
    elseif cucumber == 30 then
        doSomethingElse(1234)
    else
        DoSomethingEntirelyDifferent()
    end

    while something do
        somethingElse
    end

    return 7 + 10;
end


-- function RotateBoxes(entity)
--     entity.Rotation.angle += PI / 4 * World.deltaTime
-- end
